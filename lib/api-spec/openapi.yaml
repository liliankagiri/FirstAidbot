openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: ResQ First Aid Chatbot API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: chat
    description: Chat and NLP operations
  - name: hospitals
    description: Nearby hospital operations
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /chat/message:
    post:
      operationId: sendMessage
      tags: [chat]
      summary: Send a chat message to the ResQ chatbot
      description: Processes user message using NLP and returns first aid guidance
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatMessageBody"
      responses:
        "200":
          description: Chatbot response with first aid guidance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatResponse"

  /hospitals/nearby:
    post:
      operationId: getNearbyHospitals
      tags: [hospitals]
      summary: Get nearby hospitals based on location
      description: Returns a list of hospitals near the given coordinates
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LocationBody"
      responses:
        "200":
          description: List of nearby hospitals
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HospitalsResponse"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    ConversationTurn:
      type: object
      properties:
        role:
          type: string
          enum: [user, assistant]
        content:
          type: string
      required:
        - role
        - content

    ChatMessageBody:
      type: object
      properties:
        message:
          type: string
          description: The user's message describing the emergency or asking for first aid guidance
        sessionId:
          type: string
          description: Session identifier for conversation context
        history:
          type: array
          items:
            $ref: "#/components/schemas/ConversationTurn"
          description: Previous conversation turns for context continuity
        latitude:
          type: number
          description: User's latitude (optional, only sent when user has granted permission)
        longitude:
          type: number
          description: User's longitude (optional, only sent when user has granted permission)
      required:
        - message
        - sessionId

    ChatResponse:
      type: object
      properties:
        response:
          type: string
          description: The chatbot's first aid guidance response
        isEmergency:
          type: boolean
          description: Whether this is classified as a life-threatening emergency
        emergencyLevel:
          type: string
          enum: [low, moderate, high, critical]
          description: Severity level of the situation
        steps:
          type: array
          items:
            type: string
          description: Step-by-step first aid instructions
        sessionId:
          type: string
        callEmergency:
          type: boolean
          description: Whether to advise calling 999 (only true for critical emergencies)
        isNewIncident:
          type: boolean
          description: Whether the AI detected this as a new incident vs follow-up to the current one
        nearbyHospitals:
          type: array
          items:
            $ref: "#/components/schemas/Hospital"
          description: Nearby hospitals (only populated for critical emergencies when location is available)
      required:
        - response
        - isEmergency
        - emergencyLevel
        - steps
        - sessionId
        - callEmergency
        - isNewIncident

    LocationBody:
      type: object
      properties:
        latitude:
          type: number
        longitude:
          type: number
      required:
        - latitude
        - longitude

    Hospital:
      type: object
      properties:
        name:
          type: string
        address:
          type: string
        distance:
          type: string
        phone:
          type: string
        latitude:
          type: number
        longitude:
          type: number
      required:
        - name
        - address
        - distance

    HospitalsResponse:
      type: object
      properties:
        hospitals:
          type: array
          items:
            $ref: "#/components/schemas/Hospital"
      required:
        - hospitals
